# i18n 国际化进度

> hass-musicflow（Home Assistant 集成组件）的 i18n 需求与进度。语言方向：**中文默认 + English**。

## 整体任务需求

- 面向用户的可见文案（实体属性、服务、选项、品牌/名称覆盖等）接入 HASS 前端多语言（`hass` 国际化机制，zh-Hans / en）。
- 与主仓库 `/v1/peers`、WebSocket 对齐：实体裁剪/显示名覆盖保持一致（见后端 i18n，`MusicFlow/i18n进度.md`）。

## 当前进度：已完成（配置流 + 服务 + 异常三级 HASS 本地化）

- **配置流 / 服务**（`strings.json` + `translations/zh-Hans.json` + `en.json`）：config flow（user / zeroconf_confirm / reauth_confirm 的 title/description/data/error/abort）与自定义服务（play_content / set_play_mode / clear_queue 的 name/description/fields）早已接入 HASS 前端多语言。
- **异常（本轮新增）**：面向用户的 `HomeAssistantError` / `BrowseError` 全部改为 HASS 「exceptions 机制」——`translation_domain=DOMAIN` + `translation_key` + `translation_placeholders`，简体中文默认、英文随 HA 界面语言切换。
  - `media_player.py`：12 处（no_current_song / update_like_failed / no_current_song_for_playlist / add_to_playlist_failed / unsupported_media / unsupported_media_type / no_available_player / no_transferable_queue / fetch_queue_failed / transfer_failed / browse_failed / search_failed）；
  - `browse_media.py`：1 处（cannot_browse）。
  - **共 13 个 `exceptions` 键，zh-Hans / en 完全对齐**（`hassfest` 自动校验键集合一致性，缺失即 CI 红）。
- **保留语言：默认中文**（zh-Hans 权威副本），en 随 HA 界面语言盘点未覆盖时回退中文。

## 边界（HASS 无标准本地化机制，保持原样）

- **媒体浏览器标题**（`browse_media.py` 根菜单"歌单/专辑/艺术家/流派"、目录标题、`or "未知专辑"` 等回退、搜索"没有找到匹配的结果"/"搜索: {query}"）：这些是 `BrowseMedia.title` 动态数据，HASS 前端不自动翻译节点标题，本地化需运行时加载翻译（代价高），当前保持中文默认。
- **设备 model**（`media_player.py` `device_info.model` = "播放器组" / "AirPlay 播放器" / "DLNA 渲染器"）：设备注册表元数据，HASS `device` 类别只翻译设备**名称**（`translation_key`）不翻译 model，保持静态中文。
- `_call` 中转发的后端错误（`HomeAssistantError(str(err))`）为动态内容，保持原样。

## 关联仓库

- 主仓库 i18n 见 `MusicFlow/i18n进度.md`。
- 卡片 i18n 见 `hass-musicflow-card/i18n进度.md`。

## 关联仓库

- 主仓库 i18n 见 `MusicFlow/i18n进度.md`。
- 卡片 i18n 见 `hass-musicflow-card/i18n进度.md`。